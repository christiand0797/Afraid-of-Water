# TODO — More Enhancements

## Objective
Add quality-of-life enhancements to "Afraid of Water".

## Steps
- [x] Add in-game volume sliders (Master / SFX / Music) to the PAUSE screen
- [x] Wire volume sliders to existing volume vars + persist via localStorage
- [x] Auto-pause when the tab/app loses focus (visibilitychange)
- [x] Save lifetime stats when quitting to the main menu (goToMenu)
- [x] Refresh pause-screen toggles (Shake / Vibration / Sound + slider positions) whenever the pause menu opens
- [x] Add a Sound on/off toggle directly inside the pause menu

## Follow-up
- Run `npm test` to verify no JS errors  ✅ passed
- Push to GitHub on a `blackboxai/enhancements` branch → pushed to `main`

